# automation.py
# from selenium import webdriver
# from selenium.webdriver.common.by import By
# from selenium.webdriver.support.ui import WebDriverWait, Select
# from selenium.webdriver.support import expected_conditions as EC
import time
import streamlit as st
import time


def yookalo(Title, Description, Keyword, target_url):
    for i in range(1, 6):
        time.sleep(5)
        st.write(f"Hello {i}")  # This displays in the app
        
def pookalo(Title, Description, Keyword, target_url):
    for i in range(1, 6):
        time.sleep(5)
        st.write(f"Bye {i}")  # This displays in the app
def mookalo(Title, Description, Keyword, target_url):
    for i in range(1, 6):
        time.sleep(5)
        st.write(f"Hiee! {i}")  # This displays in the app
        
        